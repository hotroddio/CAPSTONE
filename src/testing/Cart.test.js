import { render, screen, fireEvent} from "@testing-library/react";
import App from "../App";
import "@testing-library/jest-dom";

describe("Cart component", () => {
    test("displays the details of the user cart", () => {
        //Render the Cart Component
        render(<App />);
        const cartMatch[index]?.title = screen.getByRole("paragraph", { title: /Fjallraven - Foldsack No. 1 Backpack, Fits 15 Laptops/,
    });
    expect(cartMatch[index]?.title).toBeinDocument
    });    
})